# ============================================================
# OceanTrace — Stage 1: Dataset Inspection & Image Processing
# ============================================================
# Install with:  pip install -r requirements.txt
# Python ≥ 3.10 recommended
# ============================================================

# --- Core scientific stack ---
# numpy 2.x ships prebuilt wheels for Python 3.13 on Windows (no C compiler needed)
numpy>=2.0.0
scipy>=1.13.0

# --- Geospatial / raster I/O ---
rasterio>=1.3.10          # GeoTIFF / SAR band reading
shapely>=2.0.6            # Vector geometry (spill polygons)
pyproj>=3.6.1             # CRS transformations
fiona>=1.9.6              # Vector feature I/O (shapefiles, GeoJSON)
geopandas>=0.14.4         # GeoDataFrame for vector analysis

# --- SAR-specific ---
# snappy is installed separately via SNAP/ESA toolbox;
# for pure-Python SAR metadata reading we use:
# (uncomment if SNAP is not available)
# pyrosar>=0.20.0

# --- Image processing ---
opencv-python-headless>=4.9.0.80   # SAR speckle filtering, morphology
scikit-image>=0.23.2               # Region props, thresholding helpers
Pillow>=10.3.0                     # General image I/O

# --- Deep learning (CPU baseline — GPU via CUDA extras) ---
torch>=2.3.0
torchvision>=0.18.0

# --- YOLO segmentation (Ultralytics) ---
ultralytics>=8.2.0        # YOLOv8/v11 segmentation API

# --- Visualisation ---
matplotlib>=3.9.0
seaborn>=0.13.2

# --- Notebook / exploration ---
jupyterlab>=4.2.0
ipykernel>=6.29.0
ipywidgets>=8.1.3

# --- Utilities ---
tqdm>=4.66.4
python-dotenv>=1.0.1
pyyaml>=6.0.2
rich>=13.7.1               # Pretty console output
click>=8.1.7               # CLI scaffolding for scripts

# --- Testing ---
pytest>=8.2.0
pytest-cov>=5.0.0

# ============================================================
# FUTURE STAGES (commented out — do not install yet)
# ============================================================
# --- Stage 2: Drift modelling ---
# opendrift>=1.11.0
# netCDF4>=1.7.0
# xarray>=2024.5.0
# cfgrib>=0.9.12

# --- Stage 3: AIS processing ---
# pyais>=2.8.0
# pandas>=2.2.0

# --- Stage 4: API & DB ---
# fastapi>=0.111.0
# uvicorn[standard]>=0.30.0
# sqlalchemy>=2.0.30
# asyncpg>=0.29.0
# psycopg[binary]>=3.1.19
# geoalchemy2>=0.15.1

# --- Stage 5: Frontend (managed by npm, not pip) ---
# React / TypeScript / Leaflet / DeckGL → see frontend/package.json
